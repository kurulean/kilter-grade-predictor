# CPU-only torch build -- installing from the default PyPI index pulls the
# full CUDA build (~2GB+), which is unnecessary for a small CPU inference
# server and will blow past most hosting platforms' build size/time limits.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.14.0+cpu
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
